fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
sqlalchemy==2.0.35
alembic==1.13.3
# Note: psycopg2-binary requires pg_config on Windows. Use psycopg3 or install PostgreSQL dev tools
# For Windows development, consider using: pip install psycopg[binary] or install PostgreSQL
psycopg2-binary==2.9.9
boto3==1.35.29
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.12
pytest==8.3.3
httpx==0.27.2
slowapi==0.1.9

# JWT and JWKS support
PyJWT==2.8.0
cryptography==41.0.7

# pgvector support (for vector embeddings)
# Note: pgvector extension must be installed in PostgreSQL
pgvector==0.2.4

# Embedding providers
openai==1.3.0
sentence-transformers==2.2.2

# Document extraction
PyPDF2==3.0.1
pdfplumber==0.10.0

# PDF generation
reportlab==4.0.7
